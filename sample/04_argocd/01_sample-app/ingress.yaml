apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test
spec:
  ingressClassName: "nginx"
  rules:
    - http:
      paths:
      - path: /test
        pathType: Prefix
        backend:
          service:
            name: echo-pods-argo
            port:
              number: 80
