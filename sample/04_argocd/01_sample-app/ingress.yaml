apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test
spec:
  ingressClassName: "nginx"
  rules:
  - host: 192.168.10.251
  - http:
      paths:
      - path: /test
        pathType: Prefix
        backend:
          service:
            name: echo-pods-argo
            port:
              number: 80
